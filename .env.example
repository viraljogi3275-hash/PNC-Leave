# Database path (relative to project root or absolute)
# For Railway, point this to a persistent volume (e.g., /app/data/leave_management.db)
DB_PATH=leave_management.db

# Port for the server
PORT=3000
